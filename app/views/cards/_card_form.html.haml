.modal-dialog{:role => "document"}
  .modal-content
    .modal-header
      %h5#exampleModalLongTitle.modal-title 
      %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
        %span{"aria-hidden" => "true"} ×
    .modal-body
      = form_for [@board, @card], remote: true do |f|
        .form-group
          = f.label :name, class: 'form-controll-label'
          = f.text_field :name, class: 'form-controll'
        .form-group
          = f.label :about, class: 'form-controll-label'
          = f.text_area :about, class: 'form-controll'
        .form-group
          = f.label :list_id, class: 'form-controll-label'
          = f.collection_select :list_id, @board.lists.all, :id, :name
        .form-group
          = f.label :deadline, class: 'form-controll-label'
          = f.date_select :deadline, class: 'form-controll'
        .modal-footer
          %button.btn.btn-secondary{"data-dismiss" => "modal", :type => "button"} Close
          = f.submit "Save", class: "btn btn-primary"
